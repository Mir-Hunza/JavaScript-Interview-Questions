// Question No.41 "Given an array of integers, use the `map` method to square each element and return a new array with the squared values."

let originalArray = [1, 2, 3, 4, 5];

let squaredArray = originalArray.map(num => num * num);

console.log('Original Array:', originalArray);
console.log('Squared Array:', squaredArray);
